//NOT COMPLETED

#include <stdio.h>

int		main(void)
{
	int m[1000];
	a[0] = 1;
	a[1] = 1;
	
	int n;
	scanf("%d", &n);
	
	for (int i = 0; i <= n; i++)
	{
		if (i & 1)
			m[i] = m[] + m[i / 2];		
	}
		
	return (0);		
}
